# Pixel Art Quiz Game (Pixel Quest)

一個結合復古像素風格 (Pixel Art) 與 Google Apps Script (GAS) 後端的網頁問答遊戲。

## ✨ 特色
- 🎨 **像素風格**：使用 "Press Start 2P" 字體與 CRT 復古配色。
- 👾 **動態關主**：使用 [DiceBear](https://www.dicebear.com/) API 生成像素風格關主圖片。
- 📊 **Google Sheets 整合**：題目管理與成績紀錄皆透過 Google Sheets 完成。
- ⚡ **React + Vite**：快速且現代化的前端開發體驗。

## 🚀 快速開始

### 1. 安裝與執行
```bash
# 安裝依賴
npm install

# 啟動開發伺服器
npm run dev
```

---

## 🛠️ Google Sheets & Apps Script 設定 (後端)

本專案需要配合 Google Sheets 作為資料庫。

### 步驟 1：建立 Google Sheet
1. 建立一個新的 Google Sheet。
2. 建立兩個工作表 (Tabs)，名稱分別為：
   - **`題目`**
   - **`回答`**

### 步驟 2：設定欄位
**「題目」工作表** (請依照此順序填寫第一列標題)：
| A | B | C | D | E | F | G |
|---|---|---|---|---|---|---|
| ID | 題目 | A | B | C | D | 解答 |

**「回答」工作表** (標題)：
| A | B | C | D | E | F | G |
|---|---|---|---|---|---|---|
| ID | 闖關次數 | 總分 | 最高分 | 第一次通關分數 | 花了幾次通關 | 最近遊玩時間 |

### 步驟 3：部署 Google Apps Script
1. 在 Google Sheet 中，點擊選單 **擴充功能 (Extensions)** > **Apps Script**。
2. 將本專案中的 `google_apps_script.js` 內容完整複製貼上到編輯器中。
3. 點擊上方的 **部署 (Deploy)** > **新增部署 (New deployment)**。
4. 設定如下：
   - **選取類型**：網頁應用程式 (Web app)
   - **說明**：Pixel Game API
   - **執行身分 (Execute as)**：**我 (Me)**
   - **誰可以存取 (Who has access)**：**任何人 (Anyone)** (重要！否則前端無法存取)
5. 點擊 **部署**，並複製產生的 **網頁應用程式網址 (Web App URL)**。

### 步驟 4：設定前端環境變數
1. 在專案根目錄建立 `.env` 檔案 (參考 `.env` 範例)。
2. 填入剛剛複製的網址：
   ```env
   VITE_GOOGLE_APP_SCRIPT_URL=你的_WEB_APP_URL
   VITE_PASS_THRESHOLD=3  # 通過門檻
   VITE_QUESTION_COUNT=5  # 題目數量
   ```

---

## 🚀 CI/CD: 自動部署到 GitHub Pages

本專案已設定 GitHub Actions，推送到 `main` 分支時會自動部署。

### 步驟 1：設定 GitHub Secrets
為了讓 GitHub Action 抓取到環境變數，請到 **Settings > Secrets and variables > Actions > New repository secret** 新增以下變數 (內容參考 `.env.example`)：

1. `VITE_GOOGLE_APP_SCRIPT_URL`
2. `VITE_PASS_THRESHOLD`
3. `VITE_QUESTION_COUNT`

### 步驟 2：開啟 GitHub Pages
1. 到 **Settings > Pages**。
2. 在 **Build and deployment** > **Source** 選擇 **GitHub Actions**。

### 步驟 3：設定 Base Path (重要)
如果你的專案網址是 `https://username.github.io/repo-name/`，請修改 `vite.config.js`：

```javascript
export default defineConfig({
  plugins: [react()],
  base: '/repo-name/', // 將此處修改為你的儲存庫名稱
})
```

設定完成後，推送程式碼到 `main` 分支即可觸發自動部署！

---

## 📝 測試題庫：生成式 AI 基礎知識 (可直接複製貼上)

請將以下表格內容複製並貼上到你的 **「題目」** 工作表 (從 A2 儲存格開始)。

| ID | 題目 | A | B | C | D | 解答 |
|----|------|---|---|---|---|------|
| 1 | 生成式 AI 中的 LLM 代表什麼？ | Large Language Model | Long Learning Machine | Little Logic Maker | Linear Learning Method | A |
| 2 | 下列何者是知名的 AI 去背/繪圖工具？ | Midjourney | Excel | Notepad | Calculator | A |
| 3 | 在與 AI 對話時，我們輸入的指令通常稱為什麼？ | Prompt (提示詞) | Label (標籤) | Tag (標記) | Code (代碼) | A |
| 4 | 當 AI 產生看似真實但完全錯誤的資訊時，這現象稱為什麼？ | Hallucination (幻覺) | Dreaming (作夢) | Lie (說謊) | Error 404 | A |
| 5 | ChatGPT 主要基於哪種神經網路架構？ | Transformer | CNN | RNN | LSTM | A |
| 6 | 為了讓模型適應特定領域知識，我們會進行什麼操作？ | Fine-tuning (微調) | Formatting (格式化) | Deleting (刪除) | Rebooting (重開機) | A |
| 7 | RAG 在 AI 技術中代表什麼？ | Retrieval-Augmented Generation | Real AI Generation | Robot Auto Guide | Random Answer Generator | A |
| 8 | 下列哪一個**不是**生成式 AI 的典型應用？ | 判斷電子郵件是否為垃圾郵件 (分類) | 撰寫行銷文案 | 生成數碼藝術圖 | 自動撰寫程式碼 | A |
| 9 | AI 將文字轉換為數字向量以進行處理的過程稱為什麼？ | Embedding | Downloading | Printing | Scanning | A |
| 10 | 調整哪個參數可以控制 AI 回答的創意度/隨機性？ | Temperature | Volume | Brightness | Speed | A |

---

## 📁 專案結構
```
pixel-game/
├── src/
│   ├── components/  # UI 元件
│   ├── context/     # GameContext (狀態管理)
│   ├── pages/       # 頁面 (Home, Game, Result)
│   ├── services/    # api.js (GAS 整合)
│   ├── utils/       # dicebear.js (圖片生成)
│   ├── styles/      # 全域樣式
│   ├── App.jsx      # 主要路由
│   └── main.jsx     # 入口點
├── google_apps_script.js # GAS 後端程式碼
├── index.html
└── vite.config.js
```
